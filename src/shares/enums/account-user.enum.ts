export enum Admission {
  IN_USE = 'In use',
  PAUSED = 'Paused',
  WITHDRAWN = 'Withdrawn',
}
export enum UserStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}
export enum UserRole {
  admin = 0,
  user = 1,
  teacher = 2,
  director = 3,
  team_leader = 4,
  cs = 5,
}
